extends layouts/main.pug
include mixins/videoBlock

block content

    .videos
        each item in videos
            +videoBlock({
                title:item.title,
                views:`total : ${item.views} Views`,
                videoFile:item.videoFile
            })